---
name: concept-explainer
description: 抽象概念多角度讲解助手。This skill should be used when a user provides a difficult-to-explain abstract concept (e.g. "死锁", "递归", "事务隔离级别", "闭包", "熵") and wants it explained from multiple angles, producing a 生活类比 + 示意图描述 + 最小可运行代码示例 + 学生常见误解, optionally filtered by 受众(中学生/大学生/自学者/教师) and 难度(易/中/难), finally rendered as a single self-contained, filterable HTML card set (light academic style, Source Han Serif). Applicable to teaching prep, tutoring, and self-learning across CS / math / physics / general domains.
---

# 抽象概念多角度讲解助手

把"一听就懵"的抽象概念，拆成四个互相呼应的视角讲透：**生活类比 → 示意图描述 → 最小可运行代码 → 学生常见误解**。最终渲染为一份自包含、可直接预览的浅色学术风 HTML 卡片（思源宋体），便于课堂投屏或自学。

## When to Use

- 用户给出一个难讲的概念，并要求"多角度 / 通俗 / 配图 / 配代码 / 讲给学生听"地讲解。
- 触发词示例：「讲讲死锁」「用通俗的话解释递归」「事务隔离级别怎么教」「把熵讲给高中生听」「做个概念讲解卡片，难度中、受众自学者」「帮我做个概念讲解卡片」。
- 若用户仅要纯文字解释、不要 HTML 卡片，则跳过 Step 4，直接以 Markdown 输出四段式。

## 难度 / 受众筛选（核心开关）

本 skill 的两个横切维度，贯穿全部四角度：

| 维度 | 取值 | 作用方式 |
|------|------|----------|
| **受众** | 中学生 / 大学生 / 自学者 / 教师 | **生成期**决定语气与术语；HTML 中用 `.callout[data-aud]` 给该受众专属提示，可点击切换显隐 |
| **难度** | 易 / 中 / 难 | **生成期 + 交互**双重作用：基础内容全难度可见；进阶内容包进 `.adv[data-depth="1"|"2"]`（1=中、2=难），HTML 顶部按钮按难度显隐 |

**四角度随维度变化的要点**（详见 `references/concept-patterns.md` 的"难度×受众矩阵"）：
- 受众=中学生：类比用纯生活场景、零术语；不写/极简代码；误解聚焦"字面直觉错"。
- 受众=大学生/自学者：类比可带专业语境；给最小可运行代码；误解聚焦"概念混淆"。
- 受众=教师：额外在 `.callout[data-aud="教师"]` 给"讲授引入话术 / 提问预设"。
- 难度=易：只给基础层，代码最短、误解 2–3 条。
- 难度=中：加入 `.adv[data-depth="1"]` 进阶类比/进阶代码/再加 1–2 条误解。
- 难度=难：再加 `.adv[data-depth="2"]` 时序/状态级示意图与坑点。

若用户未指定，默认 **受众=大学生、难度=中**。

## Workflow

### Step 1 — 解析概念、学科与筛选维度

接收输入（一个概念，可选附带"受众 / 难度 / 学科"）。

1. 先判断**学科范式**以选语气与代码语言：
   - **编程 / 计算机**：优先代码卡给"最小可运行示例"，类比用系统/生活场景。
   - **数学**：代码换"伪代码 / 计算示例 + 关键公式"，示意图偏结构图。
   - **物理 / 通用**：强调生活类比，代码卡给"可交互小实验"或省略。
2. 解析**受众 / 难度**（未指定则默认大学生 / 中），据此设定 Step 2 的深度与术语强度，并记入元信息。

### Step 2 — 四角度生成（核心，必须全部产出）

对每个概念，强制产出以下四块，且**四块要彼此呼应**（同一套比喻/同一段代码贯穿）：

1. **生活类比**
   - 找该概念最贴近的日常场景，用 1–2 段叙述把抽象映射到具体。
   - 关键：类比要"对应得上结构"，而不是只求生动——在类比末尾点明"哪个现象对应概念的哪个要素"。

2. **示意图描述**
   - 用文字描述一张图（可在 HTML 中落地为 ASCII 图 / 表格 / 简单 SVG）。
   - 描述要具体到"图上有几个元素、箭头/流向如何、标注什么文字"，让不会画图的人也能脑补。
   - 复杂流程优先给**顺序步骤图**或**状态流转图**。

3. **最小可运行代码示例**
   - 给出"能跑、最短、可复制"的代码（默认 Python；若概念偏前端/数据库则换语言）。
   - 必须**自包含**：含 `if __name__ == "__main__":` 或等价入口，复制即可运行并看到现象。
   - 在代码后附"运行预期输出"与 1 句"这行代码对应概念哪一点"。
   - 若概念不适合代码（纯数学/物理概念），改为"最小计算示例 + 关键公式"，并在卡片注明。

4. **学生常见误解**
   - 列 3–5 条初学者最易踩的坑，每条格式：`误解：… → 正解：…`。
   - 优先来自真实教学反馈（如"以为递归会无限循环""以为死锁只在单线程"）。

参考 `references/concept-patterns.md` 中各学科的讲解范式与"已验证示例"（死锁 / 递归 / 事务隔离级别），保持风格与保真度一致。

### Step 3 — 先输出 Markdown 版（便于检查与复制）

在渲染前，先用 Markdown 输出四段式，便于用户快速核对内容是否准确。顶部加一行元信息：`概念：X ｜ 学科：Y ｜ 受众：Z ｜ 难度：W`。若难度≥中，可把"进阶内容"用二级标题（如 `#### [进阶·中]` / `#### [进阶·难]`）与基础内容区分，便于后续映射到 `.adv` 块。

### Step 4 — 渲染可筛选 HTML 卡片（默认执行）

内容确认/生成后，**默认**渲染为一份浅色学术风、可交互筛选的 HTML 卡片：

1. 复制 `assets/concept-card-template.html` 为工作区新文件，文件名建议：`概念_<概念名>.html`（如 `概念_死锁.html`）。
2. **接好筛选开关**：
   - 控制条 `#controls` 上填 `data-default-aud="<受众>"` 与 `data-default-diff="<0|1|2>"`（易=0/中=1/难=2），页面加载即按该受众/难度呈现。
   - 头部两个 chip（受众 / 难度）文案随之更新。
3. 四个 `<section class="card">` 块填四角度内容：
   - **生活类比**：基础层用 `<p>`；进阶放 `.adv[data-depth="1"|"2"]`；该受众专属提示放 `.callout[data-aud="<受众>"]`。
   - **示意图描述**：基础 ASCII 图用 `<pre class="diagram">`；`data-depth="2"` 放时序/状态级进阶图。
   - **最小可运行代码**：`<pre><code>` 放基础代码，下方 `<p class="run">` 写运行预期；进阶代码/规避逻辑放 `.adv[data-depth="1"]`。
   - **学生常见误解**：基础 2–3 条 `<li>`；`data-depth="1"` 的 `.adv` 内再加 1–2 条。
4. 进阶块按"难度≥中显示 `data-depth=1`、仅难显示 `data-depth=2`"规则标注；**基础内容不带 `data-depth`**（始终可见）。
5. 配色与字体已内置（浅色学术风 + 思源宋体 + 学术深蓝主色），**无需改写 CSS**；筛选 JS 已内置，无需改写脚本。
6. 浏览器打开即可；如需我方启动本地服务器预览，直接告知。

若用户明确只要 Markdown、不要网页，则跳过本步骤。

## Resources

### references/

- `concept-patterns.md`：分学科（编程/数学/物理/通用）讲解范式，含"死锁 / 递归 / 事务隔离级别"三套已验证示例，作为四角度生成的保真模板。

### assets/

- `concept-card-template.html`：自包含浅色学术风 HTML 卡片模板（四卡片：生活类比 / 示意图描述 / 最小可运行代码 / 学生常见误解），内置**受众 / 难度筛选控制条**（`.callout[data-aud]` 受众专属提示 + `.adv[data-depth]` 进阶内容分层显隐），无外部依赖、中文思源宋体正常显示。
